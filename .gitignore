# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
pubspec.lock

# Platform folders are generated by `flutter create .` (see README) —
# not committed from this environment since it has no Flutter SDK to
# generate them.
linux/
macos/
windows/
web/

*.iml
.idea/
.vscode/
.DS_Store
